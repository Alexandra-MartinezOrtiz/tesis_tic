import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  standalone: true,
  template: `<p>Login (microservicio Identity — estudiante C).</p>`,
})
export class LoginComponent {}
